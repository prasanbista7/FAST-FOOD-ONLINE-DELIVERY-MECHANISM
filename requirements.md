## Packages
framer-motion | Page transitions and complex animations
recharts | Analytics charts for admin dashboard
lucide-react | Icons (already in stack, but noting usage)
clsx | Class name utility (already in stack)
tailwind-merge | Class name utility (already in stack)

## Notes
Integration with WhatsApp via simple link for customer side.
Admin dashboard simulates chat logs.
Images will use Unsplash placeholders since no upload functionality is requested yet.
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
